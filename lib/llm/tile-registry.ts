// Static export of tile registry data for better compatibility with Next.js builds
import registryData from './tile-registry.json'

export const tileRegistry = registryData
export default registryData
